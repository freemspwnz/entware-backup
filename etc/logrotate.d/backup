# Backup log rotation (used when BACKUP_LOG_FILE=/opt/var/log/backup.log)
# Copy to /opt/etc/logrotate.d/backup on the device.

/opt/var/log/backup.log {
    rotate 7
    daily
    missingok
    notifempty
    compress
    copytruncate
}
